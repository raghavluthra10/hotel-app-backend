function authHotelOwner(req, res, next) {
  try {
    //  extract id and password from req
    // verify with id if the user exsts
    // verify if the password is valid
    //  PROVIDE ACCESS ACCORDING TO THE ROLE
  } catch (error) {}
}

function authCustomer(req, res, next) {
  try {
  } catch (error) {}
}

module.exports = {
  authHotelOwner,
  authCustomer,
};
