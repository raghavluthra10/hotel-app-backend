{
  "name": "hotel-booking-app-backend",
  "version": "1.0.0",
  "description": "backend for hotel-booking-app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.js",
    "migrate": "npx sequelize-cli db:migrate",
    "down-migrate": "npx sequelize-cli db:migrate:undo",
    "create-migrate-file": "npx sequelize-cli migration:create --name"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.17.3",
    "pg": "^8.7.3",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.19.0"
  },
  "devDependencies": {
    "bcrypt": "^5.0.1",
    "nodemon": "^2.0.15",
    "sequelize-cli": "^6.4.1"
  }
}
